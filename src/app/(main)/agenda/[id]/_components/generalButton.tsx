import { Button } from '@/components/ui';

const GeneralButton = () => {
  return (
    <Button className='w-full mt-5 bg-primary-600 hover:opacity-70 text-white'>
      Submit
    </Button>
  );
};

export default GeneralButton;
